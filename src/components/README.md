# components/

- `ui/` — small, generic, reusable primitives (Button, Input, Modal, Card). No business logic, no API calls.
- `layout/` — structural pieces used across pages (Navbar, Footer, Sidebar, PageContainer, AdminShell).
- `common/` — shared but slightly more specific pieces (EmptyState, ErrorBoundary, LoadingSpinner, ProtectedRoute).
