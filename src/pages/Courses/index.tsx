import { PageContainer } from '@components/layout/PageContainer';

/**
 * Stub — real content comes in a later milestone. Exists now so routing
 * and the layout shell have something to render.
 */
export default function Courses() {
  return (
    <PageContainer className="py-16">
      <h1 className="font-heading text-2xl text-primary-dark">Our Courses</h1>
      <p className="mt-2 text-sm text-primary-dark/60">This page hasn't been built yet.</p>
    </PageContainer>
  );
}
