# features/

Feature-sliced modules for anything with real domain logic (events/, courses/, auth/, faq/, media/, admin/).
Each feature folder can contain its own components/, hooks/, api.ts, types.ts — kept local until reused elsewhere.
